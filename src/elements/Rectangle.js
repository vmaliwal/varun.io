import styled from 'styled-components';

export const RectangleWrapper = styled.div`
  border: 1px solid ${(props) => props.color};
  border-radius: 10px;
  width: 100vw;
  box-shadow: 0.6rem 0.6rem 0px ${(props) => props.color};
`;

// #3fafb9
// #36BA7B
